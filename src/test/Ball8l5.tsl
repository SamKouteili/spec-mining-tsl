always assume {
                

[ball <- moveLeft ball] -> X (! rightmost ball);
[ball <- moveRight ball] -> X (! leftmost ball);
! (leftmost ball && rightmost ball);
}
initially guarantee {
  (X (X (G ((! rightmost ball) || (G ([ball <- moveLeft ball] || (G leftmost ball)))))));
}
